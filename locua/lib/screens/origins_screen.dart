// origins_screen.dart
// Placeholder for the Origins tab. Will later show the flashcard browse
// of foreign-origin words with pronunciation (Day 9).

import 'package:flutter/material.dart';

class OriginsScreen extends StatelessWidget {
  const OriginsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Origins', style: Theme.of(context).textTheme.titleLarge),
    );
  }
}