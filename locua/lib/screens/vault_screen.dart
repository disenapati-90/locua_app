// vault_screen.dart
// Placeholder for the Vault tab. Will later show the user's personal
// text and voice mnemonics (Day 10).

import 'package:flutter/material.dart';

class VaultScreen extends StatelessWidget {
  const VaultScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Vault', style: Theme.of(context).textTheme.titleLarge),
    );
  }
}